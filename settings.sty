% required packages
\usepackage[compact]{titlesec}
\usepackage[utf8]{inputenc}
\usepackage{tabularray}
\usepackage{listings}
\usepackage{multicol}
\usepackage{textcomp}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{color}

% page geometry
\geometry{left=2cm,right=2cm,bottom=2cm,top=2cm}
% columns spacing
\setlength{\columnsep}{2em}
% tighter titles, sections, etc.
\titlespacing{\section}{0pt}{1em}{0.5em}
\titlespacing{\subsection}{0pt}{1em}{0.5em}
\titlespacing{\subsubsection}{0pt}{1em}{0.5em}
\titlespacing{\paragraph}{0pt}{1em}{0.5em}
% changed bullet for lists
\renewcommand\labelitemi{\small\bfseries\texttt{>}}
\renewcommand\labelitemii{\small\bfseries\texttt{-}}
% tighter lists
\setlist[itemize]{noitemsep, after=\vspace{0.25em}, before=\vspace{0.25em}}
\setlist[enumerate]{noitemsep, after=\vspace{0.25em}, before=\vspace{0.25em}}
% tighter lstlisting
\lstdefinestyle{tight}{
  xleftmargin=1em,
  xrightmargin=1em,
  aboveskip=0em,
  belowskip=0.5em,
  backgroundcolor=\color[rgb]{0.95,0.95,0.95},
}
% lstlisting default style
\lstset{style=tight}
% racket language
\lstdefinelanguage{Racket}{
  morekeywords={add1, and, append, apply, begin, call-with-current-continuation, call-with-input-file, call-with-output-file, call/cc, car, case, cdr, cond, cons, count, define, define-macro, define-stream, define-syntax, delay, do, else, environment, eq?, equal?, eqv?, eval, even?, export, filter, first, fold, foldl, foldr, for-each, force, if, implies, import, lambda, last, length, let, let*, let*-values, let-syntax, let-values, letrec, letrec-syntax, list, list->string, list-ref, list-tail, map, not, odd?, or, or, quasiquote, query, quote, quotient, remainder, rest, reverse, string-append, stream-lambda, string->list, string-length, string-ref, struct, sub1, syntax, syntax-rules, tring-append, true?, unquote, unquote-splicing, vector-ref, xor},
  alsodigit=!\$\%&*+-./:<=>?@^_~,
  alsoletter=!?>,
  sensitive=true,
  morecomment=[l]{;},
  morecomment=[s]{\#|}{|\#},
  morestring=[b]",
  basicstyle=\small\ttfamily,
  keywordstyle=\bf\ttfamily\color[rgb]{0,.3,.7},
  commentstyle=\color[rgb]{0.133,0.545,0.133},
  stringstyle={\color[rgb]{0.75,0.49,0.07}},
  upquote=true,
  breaklines=true,
  breakatwhitespace=true,
  literate=*{`}{{`}}{1},
  showstringspaces=false
}
% command to switch column
\newcommand{\switchcolumn}{\vspace*{\fill}\columnbreak}

